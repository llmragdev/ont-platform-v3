# 06 온톨로지 AI 업무화면 기획 추적도

## 1. 원문 문서

- `req_doc_hub/분석/06_온톨로지_AI_업무화면_기획.md`

## 2. 핵심 요구/설계

- 대시보드
- 객체 탐색
- AI 질의
- 문서 근거
- 승인 워크플로우
- 우측 컨텍스트 패널
- 실행 이력
- 업무형 애플리케이션 레이아웃

## 3. 구현 추적

### 3.1 화면 구조

`src_codex`:
- `src_codex/index.html`
- `src_codex/app.js`
- `src_codex/style.css`

`src_anti`:
- `src_anti/index.html`
- `src_anti/app.js`
- `src_anti/style.css`
- `views`
- `setupMenu()`
- `updateView()`

판단:
- `src_codex`: 부분 구현
- `src_anti`: 구현됨

### 3.2 대시보드와 객체 탐색

`src_codex`:
- API: `server.py`
- 데이터 조회: `AppContext.order_context()`, `OntologyService.customers()`, `OntologyService.orders()`

`src_anti`:
- `renderPendingOrders()`
- `renderAllOrders()`
- `selectOrder()`
- `updateContext()`

판단:
- `src_codex`: 부분 구현
- `src_anti`: 구현됨

### 3.3 AI 질의

`src_codex`:
- `AppContext.ask()`
- `RAGService.build_search_query()`
- `RAGService.build_prompt()`
- `LLMGateway.generate_rule_based_answer()`

`src_anti`:
- `handleAsk()`
- `RAGService.generate_answer()`

판단:
- 백엔드 질의 흐름: `src_codex` 구현됨
- 화면 질의 경험: `src_anti` 구현됨

### 3.4 문서 근거

`src_codex`:
- `AppContext._present_search_results()`
- `SearchService.search()`

`src_anti`:
- `handleAsk()`의 `evidenceList.innerHTML`
- `SearchService.search()`

판단:
- `src_codex`: 구현됨
- `src_anti`: 구현됨

### 3.5 워크플로우와 실행 이력

`src_codex`:
- `WorkflowService.queue()`
- `WorkflowService.execute()`
- `AuditService.list_events()`

`src_anti`:
- `executeAction()`
- `refreshAuditLogs()`
- `AuditService.log_event()`

판단:
- `src_codex`: 백엔드 구현 강함
- `src_anti`: 화면 구현 강함

## 4. 요약

`06` 문서의 화면 기획은 `src_anti`가 더 직관적으로 구현합니다. 다만 AI 질의와 권한/감사 기반 백엔드 흐름은 `src_codex`가 더 체계적입니다.

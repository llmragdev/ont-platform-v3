# 08 온톨로지 권한 거버넌스 설계 추적도

## 1. 원문 문서

- `req_doc_hub/분석/08_온톨로지_권한_거버넌스_설계.md`

## 2. 핵심 요구/설계

- 역할 모델
- 객체 권한
- 속성 권한과 마스킹
- 관계 권한
- 액션 권한
- 문서 권한
- AI 컨텍스트 권한
- 감사 로그

## 3. 구현 추적

### 3.1 역할 모델

`src_codex`:
- `src_codex/backend/data.py`
- `USERS`
- `AppContext.user()`

`src_anti`:
- `src_anti/backend/policy.py`
- `PolicyService.ROLES`
- `main.py`의 `CURRENT_ROLE`

판단:
- `src_codex`: 구현됨
- `src_anti`: 부분 구현

### 3.2 객체 권한

`src_codex`:
- `PolicyEngine.can_read_object()`
- `PolicyEngine.assert_can_read_object()`

`src_anti`:
- 객체 조회 권한은 거의 미구현

판단:
- `src_codex`: 구현됨
- `src_anti`: 미구현

### 3.3 속성 마스킹

`src_codex`:
- `PolicyEngine.mask_object()`

`src_anti`:
- `PolicyService.get_masking_required()`
- `main.py`의 고객 목록 마스킹 예시

판단:
- `src_codex`: 구현됨
- `src_anti`: 부분 구현

### 3.4 액션 권한

`src_codex`:
- `PolicyEngine.can_execute_action()`
- `PolicyEngine.available_actions()`
- `WorkflowService.execute()`

`src_anti`:
- `PolicyService.check_permission()`
- `main.py`의 `execute_workflow()`

판단:
- `src_codex`: 구현됨
- `src_anti`: 부분 구현

### 3.5 문서 권한과 AI 컨텍스트 권한

`src_codex`:
- `PolicyEngine.can_read_document()`
- `SearchService.search()`
- `AppContext.ask()`의 마스킹된 컨텍스트 사용

`src_anti`:
- 문서 권한 필터 미구현
- AI 컨텍스트 권한 미구현

판단:
- `src_codex`: 구현됨
- `src_anti`: 미구현

### 3.6 감사 로그

`src_codex`:
- `AuditService.record()`
- 접근 거부, 질의, 검색, 액션 이벤트 기록

`src_anti`:
- `AuditService.log_event()`
- 주요 API 이벤트 기록

판단:
- `src_codex`: 구현됨
- `src_anti`: 부분 구현

## 4. 요약

`08` 문서의 권한/거버넌스 설계는 `src_codex`가 훨씬 충실합니다. `src_anti`는 역할과 권한의 골격은 있으나, 실제 사용자별 객체/문서/AI 컨텍스트 권한 적용은 제한적입니다.

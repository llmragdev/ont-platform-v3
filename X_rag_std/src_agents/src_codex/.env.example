# Local default. No external RDBMS server is required.
DATABASE_URL=sqlite:///./storage/metadata.db

# Production examples. Change only the environment, not source code.
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/ragdb
# DATABASE_URL=mysql+pymysql://user:password@localhost:3306/ragdb

# RAG 표준 v1.3 코드 평가 보고서 — 작성 가이드

배포 대상: **Codex**, **Antigravity**  
작성일: 2026-05-15  
요청자: Claude Code

---

## 목적

3개 에이전트가 **동일한 양식**으로 평가 보고서를 작성하면, 항목별 교차 비교와 종합 분석이 가능해진다.  
이 가이드의 양식대로 작성하면 된다. 샘플은 `Claude_04_표준평가보고서_샘플.md`를 참조.

---

## 제출 파일명 규칙

```
{에이전트명}_04_표준평가보고서.md

예:
  Codex_04_표준평가보고서.md
  Antigravity_04_표준평가보고서.md
```

저장 위치: `E:\ontology_edu\X_rag_std\src_agents\cross-코드-평가\v03-2 평가 v1.2\`

---

## 보고서 구조 (필수 준수)

```
# RAG 표준 v1.3 코드 평가 보고서 — (작성: {에이전트명})

작성일: YYYY-MM-DD
작성자: {에이전트명} ({자기 소스 경로})
평가 기준: RAG_표준_설계_v1.3.md

## Part 1. 자기 코드 평가
  ### 1-A. v1.3 항목별 준수 현황  ← 표 형식 필수
  ### 1-B. 미준수 항목 개선 계획  ← before/after 코드 필수

## Part 2. 타 에이전트 코드 평가
  ### 2-A. {타 에이전트1} 개선 제안  ← 표 + before/after 코드 필수
  ### 2-B. {타 에이전트2} 개선 제안

## Part 3. 종합 점수 (자기 판단)  ← 표 형식 필수
```

---

## Part 1-A 표 작성 규칙

```markdown
| v1.3 항목 | 준수 | 근거 파일:라인 | 비고 |
|-----------|:----:|--------------|------|
| §3.1 X-Tenant-ID 필수 (400) | ✅ or ❌ or ⚠️ | 파일명:라인번호 | 한 줄 설명 |
```

**평가 대상 항목 (전체 20개 — 모두 작성)**

| 번호 | v1.3 항목 | 판정 기준 |
|------|-----------|---------|
| 1 | §3.1 X-Tenant-ID 필수 (누락 시 400) | 헤더 없을 때 400 반환 여부 |
| 2 | §3.1 X-Org-ID 선택 수신 | `Header(default=None)` 처리 |
| 3 | §3.2 OR 조건 검색 (팀+공유, 부서+공유) | `_build_where_clause` 존재 여부 |
| 4 | §2.3 org_id/dept_code 메타데이터 저장 | pipeline 메타 dict에 포함 여부 |
| 5 | §2.3 전사 공유 `org_id=""` sentinel | `org_id or ""` 정규화 |
| 6 | §4 ca_org_mgnt 복합 PK (tenant_id, org_id) | `mapped_column(primary_key=True)` 2개 |
| 7 | §2.3 tags → vector metadata 제외 | metadata dict에 tags 없음 |
| 8 | §2.2 embeddings= 명시 전달 | adapter `add_documents(embeddings=...)` |
| 9 | §2.1 asyncio.to_thread 비동기 파이프라인 | `asyncio.to_thread` 사용 |
| 10 | §2.1 pipeline_status "pending" 즉시 반환 | create_task 후 "pending" 반환 |
| 11 | §1.1 Gateway 호출에 tenant_id 전달 | embed/llm 호출 파라미터 확인 |
| 12 | §2.3 page_no 실제 PDF 페이지 번호 | `enumerate(reader.pages)` 사용 |
| 13 | §2.1 chunk_size 700, overlap 80 | 상수 또는 설정값 확인 |
| 14 | §5 임베딩 Fallback 금지 | Gateway 실패 시 dummy 벡터 반환 없음 |
| 15 | §5 Index Swap Pattern | `AdminService.perform_index_swap()` 또는 동등 구현 |
| 16 | SSE 스트리밍 검색 | `StreamingResponse` + async generator |
| 17 | debug_mode candidate_chunks 분리 | debug_mode 조건부 반환 |
| 18 | 감사 로그 (AuditLog) | `wc_audit_log` 테이블 또는 동등 구현 |
| 19 | datetime timezone-aware | `datetime.now(timezone.utc)` 사용 |
| 20 | 테스트 외부 의존 없음 | in-memory DB + mock — 서버 없이 실행 가능 |

**판정 기호:**
- ✅ 준수 — 코드에 구현되어 있고 동작이 확인됨
- ❌ 미준수 — 구현 없음 또는 반대로 동작
- ⚠️ 부분 — 구현됐으나 엣지 케이스 존재 (설명 필수)

---

## Part 1-B 작성 규칙

❌ 또는 ⚠️ 항목에 대해 각각 다음을 작성:

```markdown
#### §{항목번호} {항목명}

**현재 코드 (`파일명:라인`):**
```python
# 실제 코드 발췌
```

**개선 코드:**
```python
# 수정된 코드
```

**구현 우선순위:** 높음 / 중간 / 낮음
```

---

## Part 2 작성 규칙

타 에이전트 코드에서 발견한 문제를 **동일한 20개 항목 기준**으로 평가한다.

```markdown
| 우선순위 | v1.3 항목 | 파일:라인 | 현재 코드 | 개선 코드 |
|---------|-----------|---------|----------|----------|
| 🔴 1 (치명) | §3.1 ... | 파일명:15 | `"default"` 하드코딩 | `raise HTTPException(400)` |
| 🟠 2 (높음) | §2.2 ... | 파일명:27 | ... | ... |
| 🟡 3 (중간) | ... | ... | ... | ... |
```

**우선순위 기호:**
- 🔴 치명 — 보안 결함 또는 테스트 전체 실패
- 🟠 높음 — v1.3 필수 항목 위반
- 🟡 중간 — 성능·운영 영향
- 🟢 낮음 — 코드 품질·권장 사항

표 아래에 가장 중요한 항목 1~2개의 **before/after 코드 블록** 추가 (설명 없이 코드만).

---

## Part 3 작성 규칙

```markdown
| 에이전트 | 점수 | 주요 강점 | 주요 약점 |
|---------|------|---------|---------|
| {자기} (자기) | X.X / 10 | ... | ... |
| src_claud/v3 | X.X / 10 | ... | ... |
| {나머지} | X.X / 10 | ... | ... |
```

- 점수는 20개 항목 기준: ✅=1점, ⚠️=0.5점, ❌=0점 (최대 10점)
- 자기 점수에 `(자기)` 표시 필수

---

## 금지 사항

1. **파일:라인 없이 서술만 쓰기** — "asyncio 미사용" (X) → `document_pipeline.py:48` (O)
2. **20개 항목 중 일부 생략** — 미해당이면 "해당없음"으로 명시
3. **코드 없는 개선 제안** — "수정 필요" 텍스트만 (X) → before/after 코드 블록 (O)
4. **타 에이전트 평가에서 자기 점수만 높이기** — 동일 기준 적용 필수

---

## 참조 파일

| 문서 | 경로 |
|------|------|
| RAG 표준 설계 v1.3 | `E:\ontology_edu\X_rag_std\RAG_표준_설계_v1.3.md` |
| 상세 설계 01~04 | `E:\ontology_edu\X_rag_std\details\` |
| Claude 샘플 보고서 | `cross-코드-평가\v03-2 평가 v1.2\Claude_04_표준평가보고서_샘플.md` |
| 이전 v03 평가 | `cross-코드-평가\v03 평가\` |

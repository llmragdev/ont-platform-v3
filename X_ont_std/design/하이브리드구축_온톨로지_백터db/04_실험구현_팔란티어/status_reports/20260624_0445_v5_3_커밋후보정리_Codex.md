# v5.3 커밋 후보 정리

- 작성자: Codex
- 기준 시각: 2026-06-24 04:45 KST
- 목적: ANTI/Codex/Claude 작업물이 섞인 상태에서 제품 코드로 인정할 파일과 보류할 파일을 분리한다.
- 현재 판정: 커밋 후보 정리 GO / 실제 커밋은 UI 증거 보강 후 권장

## 1. 1차 커밋 후보: v5.3 gate contract

아래 파일은 v5.3 Answerability Gate, confidence policy, UI 표시 계약에 직접 연결된다.

### Backend

포함 권장:

- `ont_platform/v5/backend/app/api/adaptive_query.py`
- `ont_platform/v5/backend/app/services/vector_search.py`
- `ont_platform/v5/backend/app/services/answerability_gate.py`
- `ont_platform/v5/backend/app/services/entity_gate.py`

이유:

- `NO_ANSWER`, `GENERAL_ONLY`, `PARTIAL` 판정 연결
- `NO_ANSWER` 시 fallback answer bypass 및 sources `FILTERED/used=false` 처리
- `GENERAL_ONLY=0.55`, `PARTIAL=0.75`, `NO_ANSWER=0.0` confidence 정책
- v5.3 gate metadata payload 제공
- vector search metadata passthrough

### Frontend

포함 권장:

- `ont_platform/v5/frontend/src/components/query/AdaptiveQueryInterface.tsx`
- `ont_platform/v5/frontend/src/components/query/AnswerDisplay.tsx`
- `ont_platform/v5/frontend/src/components/query/SourcePanel.tsx`
- `ont_platform/v5/frontend/src/lib/api.ts`
- `ont_platform/v5/frontend/src/store/useQueryStore.ts`

이유:

- SSE complete meta의 `v5_3` payload 보존
- `FILTERED/used=false` source 표시
- `NO_ANSWER`, `GENERAL_ONLY`, gate decision badge 표시
- limitations/follow_ups 배열 payload 파싱 보정

### Tests

포함 권장:

- `ont_platform/v5/backend/test_gate.py`
- `ont_platform/v5/backend/test_gate_expanded.py`
- `ont_platform/v5/frontend/cypress/e2e/v5_3_hybrid_query_smoke.cy.js`

이유:

- backend gate regression 4문항
- 확장 smoke 12문항
- UI smoke 자동화 spec 보존

주의:

- Cypress 실행 환경은 아직 WAIT이다. spec 파일은 포함 가능하지만, 실행 성공 증거는 별도 확보가 필요하다.

## 2. 2차 커밋 후보: 실행 안정화

아래 파일은 v5.3 gate 자체는 아니지만, 로컬 서버 실행과 `.env` 로딩 안정화에 관련된다.

보류 후 별도 커밋 권장:

- `ont_platform/v5/backend/app/main.py`
- `ont_platform/v5/scripts/start_backend.ps1`

이유:

- `app/main.py`: `.env` 탐색 경로를 v5 backend 폴더 기준으로 조정
- `start_backend.ps1`: uvicorn 실행 시 `--env-file .env` 추가

권장 처리:

- v5.3 gate 커밋과 섞지 말고 `chore: stabilize v5 backend env loading` 같은 별도 커밋으로 분리한다.
- 서버 재시작 smoke 후 포함 여부를 결정한다.

## 3. 제외 후보

이번 v5.3 제품 커밋에서 제외:

- `ont_platform/v5/backend/phase0_report_script.py`
- 기존 대량 untracked 문서/백업/실험 산출물
- `AI_TASK_CONTROL`, `task_logs`, `demo_video`, `sheet*.csv` 등 현재 gate 변경과 무관한 산출물
- `ont_platform/v3` 관련 untracked 파일
- `ont_platform/v5/backend/data`, `performance_results`, `streamlit_apps` 등 런타임 산출물

이유:

- v5.3 answerability gate와 직접 관계 없음
- 대량 산출물 포함 시 리뷰와 롤백 범위가 흐려짐

## 4. 현재 검증 기준

통과:

```powershell
python -m py_compile app\api\adaptive_query.py app\services\answerability_gate.py app\services\entity_gate.py
python test_gate.py
python test_gate_expanded.py
```

확인된 결과:

- `document_only` trap: `NO_ANSWER`, confidence `0.0`, sources `FILTERED`
- `expert_mode`: `GENERAL_ONLY`, confidence `0.55`
- `document_with_limits`: `PARTIAL`, confidence `0.75`

미완료:

- Cypress 실제 실행: 환경 문제로 WAIT
- 실제 브라우저 화면 스크린샷: WAIT
- Gemini live answer quality: quota 429로 WAIT

## 5. 다음 액션

### Codex

1. 위 1차 커밋 후보만 대상으로 최종 diff review를 수행한다.
2. 필요하면 `git add` 대상 명령을 산출한다.
3. `main.py`, `start_backend.ps1`는 별도 실행 안정화 커밋으로 분리할지 사용자 확인 후 진행한다.

### Claude Code PM

1. `20260623_1630_V5_3_UI_SMOKE_REPORT.md`의 `GENERAL_ONLY confidence=0.85` 표현을 `0.55` 기준으로 정정한다.
2. 최종 판정을 `GO`에서 `Backend GO / UI browser evidence WAIT`로 조정한다.
3. 실제 화면 증거를 확보한다.

### Antigravity

1. `GENERAL_ONLY=0.55` 정책 재검토
2. `PARTIAL=0.75` 과신 가능성 재검토
3. `NO_ANSWER` 과차단 운영 리스크 관측 항목화


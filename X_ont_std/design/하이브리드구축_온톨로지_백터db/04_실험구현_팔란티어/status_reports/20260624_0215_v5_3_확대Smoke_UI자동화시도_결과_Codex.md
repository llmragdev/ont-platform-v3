# v5.3 확대 Smoke 및 UI 자동화 시도 결과 - Codex

**작성일:** 2026-06-24 02:15  
**작성자:** Codex  
**상태:** 확대 API smoke GO, UI 자동화 spec 작성 완료, Cypress 실행 환경 WAIT

---

## 1. 현재 서버 상태

```text
Backend: http://127.0.0.1:8001
Frontend: http://127.0.0.1:3001/hybrid-query
```

확인:

```text
Backend health: 200
Frontend route: 200
```

---

## 2. 추가한 검증 자산

### 2.1 확대 API smoke

파일:

```text
ont_platform/v5/backend/test_gate_expanded.py
```

목적:

```text
1. Snowflake류 false positive 방어 확인
2. 무관 도메인 Kafka/Kubernetes 질문 차단 확인
3. 전문가 모드 정상 생존 확인
4. document_with_limits 모드의 PARTIAL 동작 확인
5. NO_ANSWER / GENERAL_ONLY confidence 정책 확인
```

### 2.2 UI smoke Cypress spec

파일:

```text
ont_platform/v5/frontend/cypress/e2e/v5_3_hybrid_query_smoke.cy.js
```

목적:

```text
1. /hybrid-query 실제 입력/클릭 smoke
2. Snowflake 질문의 v5.3 NO_ANSWER / 0% confidence / FILTERED 문서 표시 확인
3. 전문가 모드의 v5.3 GENERAL_ONLY / 55% confidence 표시 확인
```

---

## 3. 확대 API smoke 결과

명령:

```powershell
python test_gate_expanded.py
```

결과:

```text
PASS | Snowflake evidence-page trap | document_only | NO_ANSWER | confidence=0.0 | rag=5 | filtered=5
PASS | Snowflake script trap | document_only | NO_ANSWER | confidence=0.0 | rag=5 | filtered=5
PASS | Kafka unrelated trap | document_only | NO_ANSWER | confidence=0.0 | rag=5 | filtered=5
PASS | Kubernetes unrelated trap | document_only | NO_ANSWER | confidence=0.0 | rag=5 | filtered=5
PASS | DB schema procedure | expert_mode | GENERAL_ONLY | confidence=0.55 | rag=5 | filtered=0
PASS | Defense C2 upper ontology | expert_mode | GENERAL_ONLY | confidence=0.55 | rag=5 | filtered=0
PASS | Ontology role in QA | expert_mode | GENERAL_ONLY | confidence=0.55 | rag=5 | filtered=0
PASS | Military ontology purpose | expert_mode | GENERAL_ONLY | confidence=0.55 | rag=5 | filtered=0
PASS | Knowledge graph relation | expert_mode | GENERAL_ONLY | confidence=0.55 | rag=5 | filtered=0
PASS | Document with limits schema | document_with_limits | PARTIAL | confidence=0.75 | rag=5 | filtered=0
PASS | Document with limits C2 | document_with_limits | PARTIAL | confidence=0.75 | rag=5 | filtered=0
PASS | Document with limits ontology role | document_with_limits | PARTIAL | confidence=0.75 | rag=5 | filtered=0
```

Status counts:

```text
NO_ANSWER: 4
GENERAL_ONLY: 5
PARTIAL: 3
```

판정:

```text
Expanded API smoke: GO
document_with_limits policy: GO for the tested representative questions
```

---

## 4. UI 자동화 시도 결과

### 4.1 Cypress 상태

기존 Cypress cache가 깨져 있었다.

초기 오류:

```text
Invalid or incompatible cached data (cachedDataRejected)
```

재설치 시도:

```powershell
npx cypress install
```

결과:

```text
Cypress 13.17.0 download / unzip / install 완료
```

### 4.2 Cypress 실행 실패

명령:

```powershell
npx cypress run --spec cypress/e2e/v5_3_hybrid_query_smoke.cy.js --browser chrome
```

결과:

```text
Cypress failed to start.
Cypress.exe: bad option: --smoke-test
Cypress.exe: bad option: --ping=572
```

해석:

```text
테스트 spec 문제라기보다, 현재 Windows/Cypress 실행 바이너리 환경 문제다.
UI smoke spec은 작성됐지만 자동 실행은 WAIT다.
```

---

## 5. LLM quota 상태

확대 smoke 중 Gemini quota 429가 반복 발생했다.

영향:

```text
1. Gate status / confidence / source filtering 검증에는 영향 없음
2. 답변 본문 품질은 fallback 영향 가능
3. 운영 후보 검토 전 LLM key / quota / model routing 정리가 필요
```

판정:

```text
Gate contract: GO
Answer quality with live LLM: WAIT
```

---

## 6. 현재 판정

```text
Backend API smoke: GO
Expanded 12-question API smoke: GO
document_with_limits representative policy: GO
Regression scripts: GO
Frontend route HTTP: GO
UI Cypress spec: READY
Cypress execution environment: WAIT
LLM live answer quality: WAIT
운영 배포: WAIT
커밋: WAIT
```

---

## 7. 다음 권고

### 단기

```text
1. 사용자가 브라우저에서 /hybrid-query 수동 smoke 2문항 확인
2. Cypress 실행 환경은 별도 이슈로 분리
3. 현재 v5.3 Phase 1은 내부 데모 후보로 고정
```

### 중기

```text
1. Gemini quota / 유료 키 / 모델 라우팅 정리
2. 전체 TypeScript 오류 기존 부채 정리
3. 24문항 평가 재개
```

### 커밋 후보

포함 후보:

```text
ont_platform/v5/backend/app/api/adaptive_query.py
ont_platform/v5/backend/app/services/vector_search.py
ont_platform/v5/backend/app/services/answerability_gate.py
ont_platform/v5/backend/app/services/entity_gate.py
ont_platform/v5/backend/test_gate.py
ont_platform/v5/backend/test_gate_expanded.py
ont_platform/v5/frontend/src/components/query/AdaptiveQueryInterface.tsx
ont_platform/v5/frontend/src/components/query/AnswerDisplay.tsx
ont_platform/v5/frontend/src/components/query/SourcePanel.tsx
ont_platform/v5/frontend/src/lib/api.ts
ont_platform/v5/frontend/src/store/useQueryStore.ts
ont_platform/v5/frontend/cypress/e2e/v5_3_hybrid_query_smoke.cy.js
```

제외 후보:

```text
ont_platform/v5/backend/phase0_report_script.py
```

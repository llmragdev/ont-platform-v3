# 작업 기록: v03-2 개선 제안 문서 작성

작성일: 2026-05-15  
작업자: Claude Code

## 작업 내용

`E:\ontology_edu\X_rag_std\src_agents\cross-코드-평가\v03-2 평가\` 디렉토리에 개선 제안 문서 2개 작성.

## 생성 파일

### Claude_02_src_antigravity_개선_제안.md

src_antigravity/v3의 8개 개선 항목 (src_claud/v3 대비):

1. **테스트 격리** (🔴 치명) — `pydantic_settings` 추가, Gateway mock(`conftest.py` 신설)
2. **Embedding boundary 수정** (🔴 치명) — adapter 내부 `gateway.embed_text` 제거, pipeline이 embedding 생성 후 `embeddings=`로 전달
3. **org_id="" sentinel 통일** (🟠 높음) — `None` → `""` 표준화, Chroma 전환 대비
4. **Chroma 어댑터 추가** (🟠 높음) — `ChromaVectorDbAdapter` + `VECTOR_DB_ENGINE` 환경변수
5. **pending 즉시 반환** (🟡 중간) — `asyncio.to_thread` + `asyncio.create_task` fire-and-forget
6. **datetime UTC** (🟡 중간) — `datetime.utcnow` → `datetime.now(timezone.utc)`
7. **lifespan 전환** (🟡 중간) — `@app.on_event("startup")` → `@asynccontextmanager lifespan`
8. **AuditLog 추가** (🟢 낮음) — `wc_audit_log` 테이블 + 검색 시 기록

예상 점수: 7.0 → 9.5 / 10

### Claude_03_src_codex_개선_제안.md

src_codex의 10개 개선 항목 (src_claud/v3 대비):

1. **X-Tenant-ID 필수화** (🔴 치명) — `default` 폴백 제거, `Depends(get_tenant_id)` 400 반환
2. **asyncio.to_thread 도입** (🔴 치명) — 동기 블로킹 파이프라인 → fire-and-forget
3. **org_id 계층 격리** (🟠 높음) — `X-Org-ID` 수신, OR 조건 검색 구현
4. **tags 제외** (🟠 높음) — vector metadata에서 삭제, RDBMS 전용 처리
5. **page_no 실제 번호** (🟡 중간) — chunk 인덱스 기반 → PDF 페이지 번호 기반
6. **FK 선언** (🟡 중간) — `ForeignKey`, `ForeignKeyConstraint` 추가
7. **tenant_id Gateway 전달** (🟡 중간) — `"default"` 하드코딩 제거
8. **datetime UTC** (🟡 중간) — `datetime.utcnow` → `datetime.now(timezone.utc)`
9. **company_id → tenant_id 전환** (🟡 중간) — 전체 네이밍 표준화
10. **v3 구현 착수** (🟢 낮음) — 설계 문서 기준 구현

예상 점수: 6.8 → 9.0 / 10

## 참조 소스 파일

- `src_antigravity/v3/services/vector_db.py` — embedding boundary 위반 확인
- `src_antigravity/v3/test_v3_standard.py` — Gateway 의존, scope="module" 문제 확인
- `src_antigravity/v3/core/config.py` — pydantic_settings 의존 확인
- `src_antigravity/v3/models/db_models.py` — datetime.utcnow, on_event 확인
- `src_codex/app/api/documents.py` — X-Company-ID 선택 처리 확인
- `src_codex/app/services/document_pipeline.py` — 동기 블로킹, page_no 인덱스 기반 확인
- `src_codex/app/models/db_models.py` — FK 없음, datetime.utcnow 확인

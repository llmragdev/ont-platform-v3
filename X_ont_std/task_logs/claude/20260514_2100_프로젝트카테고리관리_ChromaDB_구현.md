# 작업 기록: 프로젝트/카테고리 관리 API + ChromaDB 연동

## 작업 개요
- **작업일시**: 2026-05-14
- **요청자**: 사용자
- **배경**: AI_Agent_Mission_Directive.md 요건 — wc_project/wc_category CRUD, 벡터DB ID 관리가 src_claud v2에 없었음
- **선행 작업**: `20260514_1700_src_claud_v1v2_분리.md`, `20260514_1800_llm_gateway_구현.md`

---

## 승인된 결정사항

| 항목 | 결정 |
|------|------|
| ChromaDB 포트 | 8001 (RAG=8000, Gateway=8010 충돌 방지) |
| 기본 엔진 | 환경변수 `VECTOR_DB_ENGINE` — 기본 local_json, 운영 시 chroma |
| 컬렉션 생성 | 프로젝트 생성 시 Chroma 컬렉션 자동 생성 |

---

## 생성된 파일

| 파일 | 설명 |
|------|------|
| `v2/app/repositories/project_repo.py` | wc_project CRUD + 기본 프로젝트(000001) 시딩 |
| `v2/app/repositories/category_repo.py` | wc_category CRUD |
| `v2/app/services/project_service.py` | 프로젝트 삭제 시 문서+벡터DB cascade, chroma 컬렉션 자동 생성 |
| `v2/app/api/projects.py` | POST/GET/{code}/DELETE /api/v1/projects |
| `v2/app/api/categories.py` | POST/GET/DELETE /api/v1/categories |

## 수정된 파일

| 파일 | 변경 내용 |
|------|----------|
| `v2/app/models/db_models.py` | SQLAlchemy FK 순서 버그 수정 (ForeignKey, String → String, ForeignKey) |
| `v2/app/models/schemas.py` | Project/Category 요청·응답 스키마 추가 |
| `v2/app/core/errors.py` | ProjectNotFoundError 추가 |
| `v2/app/core/config.py` | VECTOR_DB_ENGINE, CHROMA_HOST, CHROMA_PORT 환경변수 추가 |
| `v2/app/core/events.py` | 서버 시작 시 기본 프로젝트 000001 자동 시딩 |
| `v2/app/main.py` | projects, categories 라우터 등록 |
| `v2/app/api/documents.py` | project_code Form 파라미터 추가 (기본값 000001) |
| `v2/app/services/document_service.py` | upload()에 project_code 전달 |
| `v2/app/services/router.py` | VECTOR_DB_ENGINE 환경변수 최우선 적용, chroma_host/port 사용 |
| `v2/README.md` | ChromaDB 기동 순서, 환경변수 표 추가 |
| `src_claud/README.md` | 프로젝트/카테고리 엔드포인트 테이블 추가 |
| `test_api.py` | 프로젝트 라이프사이클, 카테고리, PDF 업로드 실서버 통합 테스트 추가 |

---

## 완료 기준 (DoD)

- [x] v2 단위 테스트 14/14 통과
- [x] 모든 신규 파일 syntax 검증 통과
- [x] 프로젝트 생성 → 문서 업로드 → 삭제 cascade 로직 구현
- [x] VECTOR_DB_ENGINE=chroma 시 ChromaDB 컬렉션 자동 생성
- [x] 기본 프로젝트 000001 자동 시딩 (FK 위반 없이 기존 문서 업로드 동작)

---

## 운영 전환 방법

```bash
# ChromaDB
chroma run --host localhost --port 8001

# RAG 서버
set VECTOR_DB_ENGINE=chroma
set CHROMA_HOST=localhost
set CHROMA_PORT=8001
set EMBEDDING_PROVIDER=gemini_http
set LLM_PROVIDER=gemini_http
set LLM_GATEWAY_URL=http://localhost:8010
uvicorn app.main:app --port 8000
```
